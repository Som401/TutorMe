Remember to create a .env file in the backend folder with the required variables (URL, PORT, JWT_SECRET) before starting the application,
also execute 'npm install' to install all the necessary dependencies.
